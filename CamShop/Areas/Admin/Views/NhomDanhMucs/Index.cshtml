@model IEnumerable<Models.EF.NhomDanhMuc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tenNhom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tenNhom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.nhomID }) |
            @Html.ActionLink("Details", "Details", new { id=item.nhomID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.nhomID })
        </td>
    </tr>
}

</table>
